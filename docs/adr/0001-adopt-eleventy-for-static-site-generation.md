# Adopt Eleventy for static site generation

The Portfolio will use Eleventy to generate its multi-page, bilingual site while retaining vanilla CSS and browser JavaScript. Eleventy adds shared layouts, Markdown-based Writing, collections, localized metadata, and static routes without introducing a client-side application runtime; hand-authored duplicate HTML and Astro were rejected as respectively too difficult to maintain and more framework than the site needs.
